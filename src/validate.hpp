#ifndef VALIDATE_H
#define VALIDATE_H

//valid<HANDLE> var = captureNew<HANDLE>(); //Esta funcion no deberia aceptar structs, solo propiedades de structs
//valid<PINFORMATION> var2 = capture<PINFORMATION>(var, var.unvalid.prop1); //No deberiamos tener acceso a var.prop1

//

#endif